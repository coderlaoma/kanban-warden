name: kanban-warden
version: "0.7.0"
description: "Hermes Kanban safety plugin: warns when kanban comments/completions/blockers contain likely secrets, credentials, private keys, or connection strings."
author: "Hairou"
hooks:
  - pre_tool_call
  - transform_tool_result
provides_tools: []
